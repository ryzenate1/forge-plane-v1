ALTER TABLE panel_settings
    ADD COLUMN IF NOT EXISTS short_name TEXT NOT NULL DEFAULT 'Forge',
    ADD COLUMN IF NOT EXISTS product_name TEXT NOT NULL DEFAULT 'GamePanel',
    ADD COLUMN IF NOT EXISTS browser_title TEXT NOT NULL DEFAULT 'GamePanel',
    ADD COLUMN IF NOT EXISTS footer_text TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS logo_url TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS favicon_url TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS login_background_url TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS theme_preset TEXT NOT NULL DEFAULT 'default',
    ADD COLUMN IF NOT EXISTS default_timezone TEXT NOT NULL DEFAULT 'UTC',
    ADD COLUMN IF NOT EXISTS date_format TEXT NOT NULL DEFAULT 'yyyy-MM-dd',
    ADD COLUMN IF NOT EXISTS number_format TEXT NOT NULL DEFAULT 'en-US',
    ADD COLUMN IF NOT EXISTS currency_format TEXT NOT NULL DEFAULT 'USD',
    ADD COLUMN IF NOT EXISTS default_dashboard TEXT NOT NULL DEFAULT 'overview',
    ADD COLUMN IF NOT EXISTS landing_page TEXT NOT NULL DEFAULT 'servers',
    ADD COLUMN IF NOT EXISTS sidebar_layout TEXT NOT NULL DEFAULT 'expanded',
    ADD COLUMN IF NOT EXISTS compact_mode BOOLEAN NOT NULL DEFAULT FALSE,
    ADD COLUMN IF NOT EXISTS advanced_mode BOOLEAN NOT NULL DEFAULT FALSE,
    ADD COLUMN IF NOT EXISTS require_email_verification BOOLEAN NOT NULL DEFAULT FALSE,
    ADD COLUMN IF NOT EXISTS password_complexity TEXT NOT NULL DEFAULT 'standard',
    ADD COLUMN IF NOT EXISTS password_expiration_days INTEGER NOT NULL DEFAULT 0,
    ADD COLUMN IF NOT EXISTS session_duration_minutes INTEGER NOT NULL DEFAULT 1440,
    ADD COLUMN IF NOT EXISTS login_rate_limit_enabled BOOLEAN NOT NULL DEFAULT TRUE,
    ADD COLUMN IF NOT EXISTS login_attempt_threshold INTEGER NOT NULL DEFAULT 5,
    ADD COLUMN IF NOT EXISTS account_lockout_minutes INTEGER NOT NULL DEFAULT 15,
    ADD COLUMN IF NOT EXISTS geo_restrictions TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS api_token_ttl_days INTEGER NOT NULL DEFAULT 0,
    ADD COLUMN IF NOT EXISTS api_rotation_days INTEGER NOT NULL DEFAULT 0,
    ADD COLUMN IF NOT EXISTS allowed_origins TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS trusted_networks TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS metrics_retention_days INTEGER NOT NULL DEFAULT 30,
    ADD COLUMN IF NOT EXISTS logs_retention_days INTEGER NOT NULL DEFAULT 30,
    ADD COLUMN IF NOT EXISTS audit_retention_days INTEGER NOT NULL DEFAULT 365,
    ADD COLUMN IF NOT EXISTS metrics_sampling_rate INTEGER NOT NULL DEFAULT 100,
    ADD COLUMN IF NOT EXISTS monitoring_poll_interval_seconds INTEGER NOT NULL DEFAULT 30,
    ADD COLUMN IF NOT EXISTS email_alerts_enabled BOOLEAN NOT NULL DEFAULT FALSE,
    ADD COLUMN IF NOT EXISTS webhook_alerts_enabled BOOLEAN NOT NULL DEFAULT FALSE,
    ADD COLUMN IF NOT EXISTS discord_webhook_url TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS slack_webhook_url TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS telegram_bot_token TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS placement_strategy TEXT NOT NULL DEFAULT 'balanced',
    ADD COLUMN IF NOT EXISTS anti_affinity_rules TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS resource_reservations_enabled BOOLEAN NOT NULL DEFAULT TRUE,
    ADD COLUMN IF NOT EXISTS node_prioritization TEXT NOT NULL DEFAULT 'capacity',
    ADD COLUMN IF NOT EXISTS recovery_strategy TEXT NOT NULL DEFAULT 'manual',
    ADD COLUMN IF NOT EXISTS failover_threshold_seconds INTEGER NOT NULL DEFAULT 300,
    ADD COLUMN IF NOT EXISTS heartbeat_threshold_seconds INTEGER NOT NULL DEFAULT 60,
    ADD COLUMN IF NOT EXISTS reservation_duration_minutes INTEGER NOT NULL DEFAULT 30,
    ADD COLUMN IF NOT EXISTS reservation_cleanup_minutes INTEGER NOT NULL DEFAULT 60,
    ADD COLUMN IF NOT EXISTS capacity_buffer_percent INTEGER NOT NULL DEFAULT 10,
    ADD COLUMN IF NOT EXISTS backup_provider TEXT NOT NULL DEFAULT 'local',
    ADD COLUMN IF NOT EXISTS backup_retention_days INTEGER NOT NULL DEFAULT 7,
    ADD COLUMN IF NOT EXISTS backup_limit INTEGER NOT NULL DEFAULT 0,
    ADD COLUMN IF NOT EXISTS backup_auto_cleanup BOOLEAN NOT NULL DEFAULT TRUE,
    ADD COLUMN IF NOT EXISTS backup_encryption_enabled BOOLEAN NOT NULL DEFAULT FALSE,
    ADD COLUMN IF NOT EXISTS backup_key_rotation_days INTEGER NOT NULL DEFAULT 90;

CREATE TABLE IF NOT EXISTS panel_settings_expanded (
    id BOOLEAN PRIMARY KEY DEFAULT TRUE,
    settings JSONB NOT NULL DEFAULT '{}'::jsonb,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    CONSTRAINT panel_settings_expanded_single CHECK (id)
);

INSERT INTO panel_settings_expanded (id) VALUES (TRUE)
ON CONFLICT (id) DO NOTHING;
